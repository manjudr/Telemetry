import { libraryDispatcher } from "./dispatcher";

export const
    ALLOW_CONSOLE_LOGS = true,
    RUNNING_ENV = 'client',
    DISPATCHER = libraryDispatcher,
    VER = 3.0,
    BATCH_SIZE = 20,
    API_SLUG = '/action',
    HOST = 'https://api.ekstep.in',
    END_POINT = '/data/v3/telemetry',
    UID = 'anonymous',
    CHANNEL = 'in.ekstep',
    START_TIME = 0,
    PDATA_ID = 'in.ekstep',
    PDATA_VER = '1.0',
    ENV = 'ContentPlayer';
